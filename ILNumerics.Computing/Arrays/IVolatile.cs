using System;

namespace ILNumerics.Core.Arrays {

    /// <summary>
    /// Interface used to identify volatile array instances (not used anymore / for ONAL but remains here for now).
    /// </summary>
    internal interface IVolatile : IDisposable {
    }
}